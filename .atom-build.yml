cmd: npm start
targets:
  build-smartcontracts:
    cmd: "npm run build"
  flatten-smartcontracts:
    cmd: "npx waffle flatten"
  test-smartcontracts:
    cmd: "npm run test"
  migrate-smartcontracts:
    cmd: "npm run migrate"
  app-dev:
    cmd: "cd app && npm run dev"
  app-build:
    cmd: "cd app && npm run build"
